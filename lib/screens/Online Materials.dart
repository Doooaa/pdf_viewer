import 'package:flutter/material.dart';

class OnlineMaterials extends StatelessWidget {
  const OnlineMaterials({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("online")),
    );
  }
}